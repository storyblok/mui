import {Meta, DocsContainer} from '@storybook/addon-docs/blocks';
import {
    ThemeProvider,
} from "@mui/material";
import {CssBaseline} from "@mui/material";
import {lightTheme} from "../../index";
import {IconsGrid} from "../IconsGrid";

import * as iconsModule from "./pictograms";

<Meta
    title="Icons/Pictograms"
    parameters={{
        docs: {
            container: ({children, context}) => (
                <DocsContainer context={context}>
                    <ThemeProvider theme={lightTheme}>
                        <CssBaseline/>
                        {children}
                    </ThemeProvider>
                </DocsContainer>
            ),
        },
    }}
/>

<IconsGrid iconModule={iconsModule}/>