import {Meta, DocsContainer, ColorPalette, ColorItem} from '@storybook/addon-docs/blocks';
import {
  alpha, Box,
  Checkbox,
  Container,
  Divider,
  Grid,
  List,
  ListItem, ListItemButton, ListItemIcon, ListItemSecondaryAction,
  ListItemText, MenuItem, MenuList,
  ThemeProvider,
  Typography,
  useTheme
} from "@mui/material";
import {CssBaseline} from "@mui/material";
import {lightTheme} from "@src/theme"; import {useState} from "react"; import Button from "@mui/material/Button";

<Meta
  title="Design/Palette"
  parameters={{
    docs: {
      container: ({children, context}) => (
        <DocsContainer context={context}>
          <ThemeProvider theme={lightTheme}>
            <CssBaseline/>
            {children}
          </ThemeProvider>
        </DocsContainer>
      ),
    },
  }}
/>

export const PaletteItem = (props) => {
  const theme = useTheme()
  const {name} = props
  return (
    <Grid item
          sx={{
            p: 2,
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            flexDirection: 'column',
            width: '100%',
            backgroundColor: theme => theme.palette[name].main,
            color: theme => theme.palette[name].contrastText,
            borderRadius: '10px',
          }}
    >
      <Typography variant="subtitle1">
        {name}
      </Typography>
      <Typography variant="body2">
        {theme.palette[name].main}
      </Typography>
    </Grid>
  )
}

export const LoremIpsum = () => (
  <>
    Lorem ipsum dolor sit amet, consectetur adipiscing elit. Cras quis ullamcorper tortor, eget lobortis leo.
    Pellentesque congue eros nec metus dictum, non viverra urna fermentum. Aliquam non viverra purus, sit amet
    dictum turpis. Ut consectetur, arcu ut commodo pretium, augue magna sodales nibh, vel imperdiet leo urna in
    metus. Pellentesque elementum pharetra feugiat. Morbi a nisi massa. Etiam dolor magna, facilisis eget luctus eu,
    aliquet non ligula.
  </>
)

export const BackgroundItem = () => {
  const {palette} = useTheme()
  return (
    <Grid item>
      <ColorItem
        title="background"
        colors={{
          default: palette.background.default,
          paper: palette.background.paper,
        }}
      />
    </Grid>
  )
}

## Surfaces

<Grid container gap={5}>
  <PaletteItem name="primary"/>
  <PaletteItem name="secondary"/>
  <PaletteItem name="success"/>
  <PaletteItem name="info"/>
  <PaletteItem name="warning"/>
  <PaletteItem name="error"/>
</Grid>

## Background

<ColorPalette>

  <BackgroundItem/>
</ColorPalette>

## Divider

<Container sx={{p: 4}}>
  <Divider/>
</Container>

## Text

<Grid container spacing={2}>
  <Grid item xs={3}>
    <Typography variant="subtitle1">
      text.primary
    </Typography>
  </Grid>
  <Grid item xs={9}>
    <Typography sx={{color: 'text.primary'}}>
      <LoremIpsum/>
    </Typography>
  </Grid>
  <Grid item xs={3}>
    <Typography variant="subtitle1">
      text.secondary
    </Typography>
  </Grid>
  <Grid item xs={9}>
    <Typography sx={{color: 'text.secondary'}}>
      <LoremIpsum/>
    </Typography>
  </Grid>
  <Grid item xs={3}>
    <Typography variant="subtitle1">
      text.disabled
    </Typography>
  </Grid>
  <Grid item xs={9}>
    <Typography sx={{color: 'text.disabled'}}>
      <LoremIpsum/>
    </Typography>
  </Grid>
</Grid>
