import { Box } from '@mui/material'

export const ContentDemoArea = () => (
  <Box
    sx={{
      height: '500px',
      background: (theme) => theme.palette.grey.A100,
      borderRadius: 2,
    }}
  />
)
